import 'package:flutter/material.dart';

class Pruebas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('sHello There'),
      ),
    );
  }
}
